//因为个人习惯，在灯笼代码的基础上作了些修改
#include <cstdio>
#include <cstdlib>
#include <cstring>

void prefix_table(char pattern[], int prefix[]) {
    int len   = strlen(pattern);
    prefix[0] = 0;
    int ptr   = 0;

    int i = 1;
    while (i < len) {

    }
}

int main() {
    char pattern[] = "ABABCABAA";
    char text[]    = "ABABABCABAABABAABAB";

    return 0;
}
